{
  "name": "pol-slots-amoy",
  "version": "1.0.0",
  "private": true,
  "description": "MVP tragamonedas on-chain en Polygon Amoy (sin backend, sin NFTs)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "verify:amoy": "hardhat run scripts/verify.js --network amoy"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.18",
    "vite": "^5.4.11"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "ethers": "^6.13.4"
  }
}
